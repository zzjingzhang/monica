# 外部地址簿订阅流程分析

## 概述

本文档详细分析 Monica 项目中外部地址簿订阅的创建、定期同步以及本地与远端之间的任务分发机制。

---

## 1. CreateAddressBookSubscription：权限与能力探测

### 1.1 权限验证

[CreateAddressBookSubscription.php](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Domains/Contact/DavClient/Services/CreateAddressBookSubscription.php) 定义了严格的权限检查机制：

```php
public function permissions(): array
{
    return [
        'author_must_belong_to_account',
        'author_must_be_in_vault',
        'author_must_be_vault_manager',
        'vault_must_belong_to_account',
    ];
}
```

**权限要求说明：**

| 权限 | 说明 |
|------|------|
| `author_must_belong_to_account` | 操作者必须属于指定的账户 |
| `author_must_be_in_vault` | 操作者必须在目标保险库中 |
| `author_must_be_vault_manager` | **关键权限**：操作者必须是保险库管理员 |
| `vault_must_belong_to_account` | 目标保险库必须属于指定账户 |

### 1.2 能力探测流程

地址簿能力探测在 [AddressBookGetter.php](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Domains/Contact/DavClient/Services/Utils/AddressBookGetter.php) 中实现，主要分为以下步骤：

#### 1.2.1 URI 发现机制

1. **逐层路径探测**：从用户提供的基础 URI 开始，逐层向上查找地址簿
2. **Principal 获取**：通过 `{DAV:}current-user-principal` 获取当前用户主体
3. **地址簿主页发现**：通过 `{urn:ietf:params:xml:ns:carddav}addressbook-home-set` 获取地址簿主页
4. **地址簿资源定位**：通过 `{DAV:}resourcetype` 识别 CardDAV 地址簿

#### 1.2.2 服务器合规性检查

```php
private function checkOptions(bool $addressbook = false, string $url = ''): void
{
    $options = $this->client->options($url);
    if (! in_array('1', $options) || ! in_array('3', $options) || 
        ($addressbook && ! in_array('addressbook', $options))) {
        throw new DavServerNotCompliantException(...);
    }
}
```

检查项包括：
- DAV 版本 1 支持（RFC 2518）
- DAV 版本 3 支持
- addressbook 扩展支持（RFC 6352）

#### 1.2.3 能力属性探测

| 能力 | 探测方式 | 用途 |
|------|----------|------|
| `addressbookMultiget` | `supported-report-set` | 批量获取多个 vCard |
| `addressbookQuery` | `supported-report-set` | 查询过滤地址簿内容 |
| `syncCollection` | `supported-report-set` | 增量同步集合变更 |
| `addressData` | `supported-address-data` | 支持的 vCard 版本（优先 4.0，降级 3.0） |

#### 1.2.4 Google API 特殊处理

```php
if (Str::startsWith($uri, 'https://www.googleapis.com')) {
    $capabilities = [
        'addressbookMultiget' => true,
        'addressbookQuery' => true,
        'syncCollection' => true,
        'addressData' => ['content-type' => 'text/vcard', 'version' => '3.0'],
    ];
}
```

Google 的 CardDAV 实现不完全符合标准，因此采用硬编码的能力声明。

---

## 2. UpdateAddressBooks：active 订阅与 frequency 筛选

[UpdateAddressBooks.php](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Domains/Contact/DavClient/Jobs/UpdateAddressBooks.php) 负责定期触发同步任务。

### 2.1 Active 订阅筛选

```php
AddressBookSubscription::active()
    ->chunkById(200, fn (Collection $subscriptions) => $this->manageSubscriptions($subscriptions, $now));
```

通过模型的 `scopeActive` 作用域筛选：

```php
// [AddressBookSubscription.php#L175-L178](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Models/AddressBookSubscription.php#L175-L178)
public function scopeActive(Builder $query): Builder
{
    return $query->where('active', 1);
}
```

### 2.2 Frequency 频率控制

```php
private function isTimeToRunSync($subscription, Carbon $now): bool
{
    return $subscription->last_synchronized_at === null
        || $subscription->last_synchronized_at->clone()
            ->addMinutes($subscription->frequency)->lessThan($now);
}
```

**触发条件：**
- 从未同步过（`last_synchronized_at` 为 null）
- 上次同步时间 + 频率间隔 < 当前时间

**性能优化：**
- 使用 `chunkById(200)` 分批处理，避免内存溢出
- 符合条件的订阅通过 `SynchronizeAddressBooks::dispatch()` 分发到 `high` 队列

---

## 3. SynchronizeAddressBook：ClientException 异常处理

[SynchronizeAddressBook.php](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Domains/Contact/DavClient/Services/SynchronizeAddressBook.php) 中的异常处理策略：

### 3.1 异常捕获与日志记录

```php
try {
    return app(AddressBookSynchronizer::class)
        ->withSubscription($this->subscription)
        ->execute($force);
} catch (ClientException $e) {
    Log::channel('database')->error(__CLASS__.' '.__FUNCTION__.': '.$e->getMessage(), [
        'body' => $e->hasResponse() ? $e->getResponse()->getBody() : null,
        $e,
    ]);
}
```

### 3.2 处理策略

1. **静默失败**：捕获异常后不抛出，仅记录日志，保证其他同步任务不受影响
2. **详细日志**：
   - 错误消息
   - HTTP 响应体（如有）
   - 完整异常堆栈
3. **订阅状态检查前置**：在同步前先检查 `$this->subscription->active`，非活跃订阅直接返回 `null`

---

## 4. AddressBookSynchronizer：同步策略选择

[AddressBookSynchronizer.php](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Domains/Contact/DavClient/Services/Utils/AddressBookSynchronizer.php) 根据服务器能力选择最优同步策略。

### 4.1 策略决策树

```
getDistantEtags()
    │
    ├─ hasCapability('syncCollection') ?
    │   ├─ YES → callSyncCollectionWhenNeeded()
    │   │       └─ sync-token 未变化 → 返回空数组
    │   │       └─ sync-token 变化 → callSyncCollection()
    │   │
    │   └─ NO  → propFind()
```

### 4.2 三种同步策略对比

| 策略 | 依赖能力 | 优点 | 缺点 | 适用场景 |
|------|----------|------|------|----------|
| **syncCollection** | `syncCollection` | 增量同步、带宽小、效率高 | 依赖服务器支持 | 现代 CardDAV 服务器 |
| **addressbookQuery** | `addressbookQuery` | 可获取全量 etag 列表 | 需遍历所有资源 | 强制同步（force=true） |
| **propFind** | 基础 DAV | 兼容性最好 | 全量查询、效率低 | 不支持高级特性的服务器 |

### 4.3 策略实现细节

#### 4.3.1 syncCollection 策略

```php
private function callSyncCollectionWhenNeeded(): array
{
    $currentSyncToken = $this->client->getProperty('{DAV:}sync-token');
    
    if (($this->subscription->distant_sync_token ?? '') === $currentSyncToken) {
        return []; // 无变更，跳过同步
    }
    
    return $this->callSyncCollection();
}
```

#### 4.3.2 propFind 回退策略

```php
private function getDistantEtags(): array
{
    return $this->hasCapability('syncCollection')
        ? $this->callSyncCollectionWhenNeeded()
        : $this->client->propFind([
            '{DAV:}getcontenttype',
            '{DAV:}getetag',
        ], 1);
}
```

### 4.4 强制同步模式

当 `force=true` 时：
1. 获取本地所有联系人 UUID
2. 通过 `addressbookQuery` 获取远端所有 etag
3. 计算差异（本地缺失 = 需要拉取）
4. 执行 `PrepareJobsContactPushMissed` 处理遗漏推送

---

## 5. PrepareJobsContactPush：过滤用户自身联系人的原因

[PrepareJobsContactPush.php](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Domains/Contact/DavClient/Services/Utils/PrepareJobsContactPush.php) 中的过滤逻辑：

### 5.1 过滤实现

```php
private function filterContacts(Collection $contacts): Collection
{
    return $contacts->reject(fn (string $uri): bool => 
        $this->getContactInVault() === $this->backend()->getUuid($uri)
    );
}

private function getContactInVault(): ?string
{
    $entry = $this->subscription->user->vaults()
        ->wherePivot('vault_id', $this->subscription->vault_id)
        ->first();
    
    return optional(optional($entry)->pivot)->contact_id;
}
```

### 5.2 过滤原因分析

**核心逻辑**：用户在 Vault 中的关联联系人（自己的联系人记录）不会被推送到远端服务器。

#### 设计意图

1. **防止循环同步**：
   - 场景：用户 A 的联系人信息存在于远端 CardDAV 服务器
   - 拉取后在本地创建联系人记录
   - 如果不排除，该联系人会被再次推送回远端
   - 导致无限循环的同步回路

2. **保护隐私/所有权**：
   - 用户自己的联系人卡片是"根记录"
   - 不应被外部地址簿修改或反向同步
   - 保持用户数据的主控权

3. **避免数据冲突**：
   - 远端地址簿可能已有该用户的不同版本信息
   - 排除自身可避免双向修改产生的冲突

### 5.3 适用场景

该过滤应用于所有推送操作：
- `preparePushAddedContacts()` - 新增联系人
- `prepareDeletedContacts()` - 删除联系人  
- `preparePushChangedContacts()` - 修改联系人

---

## 6. AddressBookSubscription：sync_way 访问器边界测试点

[AddressBookSubscription.php](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Models/AddressBookSubscription.php) 中的同步方向控制使用位运算。

### 6.1 常量定义

```php
public const WAY_PUSH = 0x1;   // 二进制: 01
public const WAY_GET  = 0x2;   // 二进制: 10
public const WAY_BOTH = 0x3;   // 二进制: 11 (WAY_GET | WAY_PUSH)
```

### 6.2 访问器实现

```php
public function isWayPush(): Attribute
{
    return Attribute::make(
        get: fn (?bool $value, array $attributes) => 
            ($attributes['sync_way'] & self::WAY_PUSH) === self::WAY_PUSH,
    );
}

public function isWayGet(): Attribute
{
    return Attribute::make(
        get: fn (?bool $value, array $attributes) => 
            ($attributes['sync_way'] & self::WAY_GET) === self::WAY_GET,
    );
}

// Bug: syncWay 应为 sync_way
public function isWayBoth(): Attribute
{
    return Attribute::make(
        get: fn (?bool $value, array $attributes) => 
            ($attributes['syncWay'] & self::WAY_BOTH) === self::WAY_BOTH,
    );
}
```

### 6.3 值得测试的边界情况

#### 6.3.1 位运算边界值

| sync_way 值 | isWayPush | isWayGet | isWayBoth | 说明 |
|------------|-----------|----------|-----------|------|
| `0` (0b00) | false | false | false | 无同步 |
| `1` (0b01) | true | false | false | 仅推送 |
| `2` (0b10) | false | true | false | 仅拉取 |
| `3` (0b11) | true | true | **false\*** | 双向同步 |
| `4` (0b100) | false | false | false | 无效高位值 |
| `> 3` | 取决于低位 | 取决于低位 | **false\*** | 溢出值 |

**注意**：`isWayBoth` 当前实现有 bug，使用了 `syncWay` 而非 `sync_way`，导致永远返回 false。

#### 6.3.2 数据库边界场景

| 场景 | 预期行为 | 测试点 |
|------|----------|--------|
| sync_way 为 NULL | 按 0 处理？数组访问是否报错 | `$attributes['sync_way']` 不存在的情况 |
| sync_way 为字符串 `"3"` | PHP 弱类型转换问题 | 字符串与数字的位运算结果 |
| sync_way 为负数 | 二进制补码的位运算 | 负数 & WAY_PUSH 的结果 |
| 并发写入 sync_way | 脏读导致的判断错误 | 读写一致性 |

#### 6.3.3 业务逻辑边界

| 测试场景 | 验证点 |
|----------|--------|
| WAY_GET 关闭时 | `PrepareJobsContactUpdater` 不被调用 |
| WAY_PUSH 关闭时 | `PrepareJobsContactPush` 不被调用 |
| sync-token 更新条件 | 仅 WAY_PUSH 开启时才更新本地同步令牌 |
| 动态变更 sync_way | 运行中修改订阅方向的即时生效性 |

#### 6.3.4 isWayBoth 的特殊问题

**代码缺陷**：
```php
// 当前代码使用了错误的键名 syncWay
($attributes['syncWay'] & self::WAY_BOTH) === self::WAY_BOTH

// 应该使用 sync_way
($attributes['sync_way'] & self::WAY_BOTH) === self::WAY_BOTH
```

**影响**：`isWayBoth` 访问器永远返回 `false`，任何依赖此判断的逻辑都会异常。

**修复建议**：将 `syncWay` 更正为 `sync_way`。

---

## 附录：整体流程架构图

```
┌─────────────────────────────────────────────────────────────────────┐
│                        定期调度层                                    │
│  UpdateAddressBooks (每分钟运行)                                     │
│    └─ active() scope + frequency 过滤                                │
│       └─ SynchronizeAddressBooks dispatch to high queue             │
└────────────────────────────────┬────────────────────────────────────┘
                                 │
                                 ▼
┌─────────────────────────────────────────────────────────────────────┐
│                        同步服务层                                    │
│  SynchronizeAddressBook                                              │
│    └─ ClientException 捕获 + 数据库日志                               │
│       └─ AddressBookSynchronizer.execute()                          │
└────────────────────────────────┬────────────────────────────────────┘
                                 │
              ┌──────────────────┴──────────────────┐
              ▼                                     ▼
┌─────────────────────────────┐     ┌─────────────────────────────┐
│  策略选择层                  │     │  方向控制层                  │
│  - syncCollection (优先)     │     │  sync_way 位掩码            │
│  - addressbookQuery (强制)   │     │  ├─ WAY_GET (0x2)           │
│  - propFind (兼容)           │     │  └─ WAY_PUSH (0x1)          │
└──────────────┬──────────────┘     └──────────────┬──────────────┘
               │                                    │
               ▼                                    ▼
┌─────────────────────────────┐     ┌─────────────────────────────┐
│  拉取任务                    │     │  推送任务                    │
│  PrepareJobsContactUpdater   │     │  PrepareJobsContactPush     │
│  - GetMultipleVCard          │     │  - PushVCard (新增/修改)    │
│  - DeleteMultipleVCard       │     │  - DeleteVCard              │
│                              │     │  - 排除用户自身联系人        │
└─────────────────────────────┘     └─────────────────────────────┘
```

---

## 关键代码文件索引

| 文件 | 核心职责 |
|------|----------|
| [AddressBookSubscription.php](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Models/AddressBookSubscription.php) | 订阅模型、sync_way 访问器 |
| [CreateAddressBookSubscription.php](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Domains/Contact/DavClient/Services/CreateAddressBookSubscription.php) | 创建订阅、权限检查 |
| [AddressBookGetter.php](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Domains/Contact/DavClient/Services/Utils/AddressBookGetter.php) | 能力探测、URI 发现 |
| [UpdateAddressBooks.php](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Domains/Contact/DavClient/Jobs/UpdateAddressBooks.php) | 定期调度、频率控制 |
| [SynchronizeAddressBook.php](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Domains/Contact/DavClient/Services/SynchronizeAddressBook.php) | 同步入口、异常处理 |
| [AddressBookSynchronizer.php](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Domains/Contact/DavClient/Services/Utils/AddressBookSynchronizer.php) | 策略选择、任务编排 |
| [PrepareJobsContactPush.php](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Domains/Contact/DavClient/Services/Utils/PrepareJobsContactPush.php) | 推送任务生成、自身过滤 |
