# 联系人任务模块流程分析

## 一、整体流程概览

联系人任务模块的完整数据流涵盖以下核心环节：

```
任务创建 → 列表聚合 → 状态切换 → Vault级视图展示
    ↓          ↓            ↓               ↓
CreateContactTask → ContactModuleTaskController → ToggleContactTask → VaultTasksIndexViewHelper
```

---

## 二、CreateContactTask：归属校验与默认状态

### 2.1 归属校验机制

[CreateContactTask.php](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Domains/Contact/ManageTasks/Services/CreateContactTask.php) 定义了严格的多层级归属校验：

**校验规则**（[L19-L30](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Domains/Contact/ManageTasks/Services/CreateContactTask.php#L19-L30)）：
- `account_id`: 必须存在于 accounts 表
- `vault_id`: 必须存在于 vaults 表  
- `author_id`: 必须存在于 users 表
- `contact_id`: 必须存在于 contacts 表

**权限校验**（[L35-L43](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Domains/Contact/ManageTasks/Services/CreateContactTask.php#L35-L43)）：
```php
'author_must_belong_to_account',    // 作者必须属于该账户
'vault_must_belong_to_account',     // Vault必须属于该账户
'author_must_be_vault_editor',      // 作者必须是Vault的编辑者
'contact_must_belong_to_vault',     // 联系人必须属于该Vault
```

### 2.2 默认状态

数据库迁移 [2022_05_13_201216_create_contact_tasks_table.php](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/database/migrations/2022_05_13_201216_create_contact_tasks_table.php) 定义：
```php
$table->boolean('completed')->default(false);      // 默认未完成
$table->datetime('completed_at')->nullable();      // 默认null
```

创建逻辑 [CreateContactTask.php:L59-L69](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Domains/Contact/ManageTasks/Services/CreateContactTask.php#L59-L69) 中，`create()` 方法未显式设置 `completed` 和 `completed_at`，因此新创建的任务：
- `completed = false`
- `completed_at = null`

---

## 三、ContactModuleTaskController：服务调用流程

[ContactModuleTaskController.php](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Domains/Contact/ManageTasks/Web/Controllers/ContactModuleTaskController.php) 作为HTTP入口，负责组装参数并调用相应的服务类。

### 3.1 主要方法与服务映射

| 控制器方法 | 路由 | 调用服务 | 主要职责 |
|-----------|------|---------|---------|
| `index()` | GET /vaults/{vault}/contacts/{contact}/tasks | - | 获取已完成任务列表 |
| `store()` | POST /vaults/{vault}/contacts/{contact}/tasks | [CreateContactTask](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Domains/Contact/ManageTasks/Services/CreateContactTask.php) | 创建新任务 |
| `update()` | PUT /vaults/{vault}/contacts/{contact}/tasks/{task} | UpdateContactTask | 更新任务 |
| `toggle()` | PATCH /vaults/{vault}/contacts/{contact}/tasks/{task}/toggle | [ToggleContactTask](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Domains/Contact/ManageTasks/Services/ToggleContactTask.php) | 切换完成状态 |
| `destroy()` | DELETE /vaults/{vault}/contacts/{contact}/tasks/{task} | DestroyContactTask | 删除任务 |

### 3.2 参数组装模式

以 `toggle()` 方法 [L78-L96](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Domains/Contact/ManageTasks/Web/Controllers/ContactModuleTaskController.php#L78-L96) 为例：
```php
$data = [
    'account_id' => Auth::user()->account_id,
    'author_id' => Auth::id(),
    'vault_id' => $vaultId,
    'contact_id' => $contactId,
    'contact_task_id' => $taskId,
    'label' => $request->input('label'),
    'description' => null,
];

$task = (new ToggleContactTask)->execute($data);
```

---

## 四、ToggleContactTask：状态切换逻辑与核心问题

### 4.1 核心实现

[ToggleContactTask.php](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Domains/Contact/ManageTasks/Services/ToggleContactTask.php) 的 `execute()` 方法 [L44-L59](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Domains/Contact/ManageTasks/Services/ToggleContactTask.php#L44-L59)：

```php
public function execute(array $data): ContactTask
{
    $this->validateRules($data);

    $this->task = $this->contact->tasks()
        ->findOrFail($data['contact_task_id']);

    $this->task->completed = ! $this->task->completed;
    $this->task->completed_at = Carbon::now();  // 关键行
    $this->task->save();

    $this->contact->last_updated_at = Carbon::now();
    $this->contact->save();

    return $this->task;
}
```

### 4.2 关键问题分析

**问题结论：当任务从已完成切回未完成时，`completed_at` 不会被清空，而是被更新为当前时间。**

具体行为分析：

| 切换方向 | `completed` 变化 | `completed_at` 变化 |
|---------|-----------------|--------------------|
| 未完成 → 已完成 | `false` → `true` | `null` → `Carbon::now()` |
| 已完成 → 未完成 | `true` → `false` | `旧时间` → `Carbon::now()`（更新而非清空） |

这意味着**未完成的任务也可能拥有非空的 `completed_at` 值**。

### 4.3 单元测试验证

[ToggleContactTaskTest.php](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/tests/Unit/Domains/Contact/ManageTasks/Services/ToggleContactTaskTest.php) 中的测试仅验证了 `completed` 字段从 `false` 变为 `true` 的情况（[L126-L130](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/tests/Unit/Domains/Contact/ManageTasks/Services/ToggleContactTaskTest.php#L126-L130)），但**未测试从已完成切回未完成的场景**，也未验证 `completed_at` 字段的行为。

---

## 五、字段消费机制

### 5.1 ContactTask 模型

[ContactTask.php](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Models/ContactTask.php) 提供了两个本地作用域：

```php
#[Scope]
public function notCompleted($query)
{
    return $query->where('completed', false);
}

#[Scope]
public function completed($query)
{
    return $query->where('completed', true);
}
```

模型始终**以 `completed` 布尔字段作为判断依据**，而非 `completed_at`。

### 5.2 ModuleContactTasksViewHelper

[ModuleContactTasksViewHelper.php](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Domains/Contact/ManageTasks/Web/ViewHelpers/ModuleContactTasksViewHelper.php)

**已完成任务列表** [L42-L49](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Domains/Contact/ManageTasks/Web/ViewHelpers/ModuleContactTasksViewHelper.php#L42-L49)：
```php
public static function completed(Contact $contact, User $user): Collection
{
    return $contact->tasks()
        ->completed()  // 先按 completed=true 过滤
        ->orderBy('completed_at', 'desc')  // 再按完成时间排序
        ->get()
        ->map(fn ($task) => self::dtoTask($contact, $task, $user));
}
```

**DTO输出** [L51-L82](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Domains/Contact/ManageTasks/Web/ViewHelpers/ModuleContactTasksViewHelper.php#L51-L82)：
```php
'completed' => $task->completed,
'completed_at' => $task->completed_at !== null ? DateHelper::format($task->completed_at, $user) : null,
```

### 5.3 VaultTasksIndexViewHelper

[VaultTasksIndexViewHelper.php](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Domains/Vault/ManageTasks/Web/ViewHelpers/VaultTasksIndexViewHelper.php) 的 `data()` 方法 [L13-L64](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Domains/Vault/ManageTasks/Web/ViewHelpers/VaultTasksIndexViewHelper.php#L13-L64)：

```php
$tasksCollection = $contact->tasks()
    ->notCompleted()  // 只查询未完成任务
    ->orderBy('due_at', 'asc')  // 按截止时间排序，不使用 completed_at
    ->get()
    ->map(fn (ContactTask $task) => [
        'id' => $task->id,
        'label' => $task->label,
        'due_at' => $task->due_at !== null ? [...] : null,
        // 注意：这里不输出 completed_at 字段
    ]);
```

### 5.4 VaultShowViewHelper

[VaultShowViewHelper.php](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Domains/Vault/ManageVault/Web/ViewHelpers/VaultShowViewHelper.php) 的 `dueTasks()` 方法 [L118-L166](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Domains/Vault/ManageVault/Web/ViewHelpers/VaultShowViewHelper.php#L118-L166)：

```php
->where('completed', false)  // 只查询未完成任务
->map(fn (ContactTask $task) => [
    'completed' => $task->completed,
    'completed_at' => $task->completed_at !== null ? DateHelper::format($task->completed_at, $user) : null,
    // 虽然只查询未完成任务，但仍输出 completed_at 字段
])
```

---

## 六、对按完成时间展示与统计的影响

### 6.1 当前受影响的功能点

| 功能点 | 影响程度 | 说明 |
|-------|---------|------|
| `ModuleContactTasksViewHelper::completed()` | 无直接影响 | 先通过 `completed()` 作用域过滤，再按 `completed_at` 排序，未完成任务不会进入列表 |
| `VaultTasksIndexViewHelper::data()` | 无影响 | 只查询未完成任务，不使用 `completed_at` 字段 |
| `VaultShowViewHelper::dueTasks()` | 潜在数据不一致 | 未完成任务可能带有 `completed_at` 值输出给前端 |

### 6.2 潜在风险

1. **前端展示逻辑风险**：如果前端代码仅通过判断 `completed_at !== null` 来决定是否显示完成状态或完成时间，会出现"未完成但显示完成时间"的异常。

2. **统计逻辑风险**：如果存在基于 `completed_at` 进行的统计（如"本周完成任务数"），且未同时校验 `completed = true`，则会将"已取消完成"的任务错误统计在内。

3. **数据一致性风险**：`completed = false` 但 `completed_at != null` 的数据状态违反了常规的业务预期，可能导致后续开发时的逻辑错误。

4. **排序语义模糊**：当任务从已完成切回未完成再重新标记为完成时，`completed_at` 会被多次更新，无法准确反映"最初完成时间"或"最后一次完成时间"的语义。

### 6.3 建议修复方案

修改 [ToggleContactTask.php](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Domains/Contact/ManageTasks/Services/ToggleContactTask.php#L51-L53)：

```php
// 当前实现（有问题）
$this->task->completed = ! $this->task->completed;
$this->task->completed_at = Carbon::now();
$this->task->save();

// 建议修改
$this->task->completed = ! $this->task->completed;
$this->task->completed_at = $this->task->completed ? Carbon::now() : null;
$this->task->save();
```

这样修改后：
- 未完成 → 已完成：`completed_at` 设置为当前时间
- 已完成 → 未完成：`completed_at` 清空为 `null`

---

## 七、总结

| 问题 | 结论 |
|-----|------|
| CreateContactTask 归属校验 | 4层校验：账户、Vault、权限、联系人归属 |
| CreateContactTask 默认状态 | `completed=false`, `completed_at=null` |
| ToggleContactTask 行为 | **从已完成切回未完成时，`completed_at` 不会被清空，而是被更新为当前时间** |
| 字段消费主体 | 以 `completed` 布尔字段为主要判断依据 |
| 对展示统计的影响 | 存在潜在的数据不一致风险，建议修复 `completed_at` 的赋值逻辑 |
