# 联系人贷款模块技术分析文档

## 概述

本文档深入分析 Monica 项目中联系人贷款（Loan）模块的核心实现机制，涵盖贷款创建、联系人关联、状态切换及金额处理等关键技术点。

---

## 一、贷款创建流程（CreateLoan）

### 1.1 服务入口与权限验证

[CreateLoan.php](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Domains/Contact/ManageLoans/Services/CreateLoan.php) 服务继承自 [BaseService.php](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Services/BaseService.php)，执行流程如下：

```php
public function execute(array $data): Loan
{
    $this->data = $data;
    $this->validate();      // 规则验证 + 权限验证
    $this->create();        // 创建贷款 + 关联联系人
    
    $this->contact->last_updated_at = Carbon::now();
    $this->contact->save();
    
    return $this->loan;
}
```

**权限规则**（[CreateLoan.php:L48-L56](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Domains/Contact/ManageLoans/Services/CreateLoan.php#L48-L56)）：
- `author_must_belong_to_account`：操作者必须属于该账户
- `vault_must_belong_to_account`：保险库必须属于该账户
- `author_must_be_vault_editor`：操作者必须有保险库编辑权限
- `contact_must_belong_to_vault`：当前联系人必须属于该保险库

---

### 1.2 loaner_id 和 loanee_id 的同一 Vault 验证机制

**核心验证逻辑**位于 [CreateLoan.php:L73-L82](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Domains/Contact/ManageLoans/Services/CreateLoan.php#L73-L82)：

```php
private function validate(): void
{
    $this->validateRules($this->data);

    $this->loanersCollection = collect($this->data['loaner_ids'])
        ->map(fn (string $id): Contact => $this->vault->contacts()->findOrFail($id));

    $this->loaneesCollection = collect($this->data['loanee_ids'])
        ->map(fn (string $id): Contact => $this->vault->contacts()->findOrFail($id));
}
```

**验证原理**：
1. `$this->vault` 已在 `BaseService` 的权限验证阶段（`validateContactBelongsToVault`）被正确实例化，确保属于当前账户
2. 通过 `$this->vault->contacts()` 关联关系查询联系人，而非直接用 `Contact::find()`
3. `findOrFail()` 确保联系人必须存在于该 vault 的联系人列表中
4. 若任一 ID 不属于当前 vault，将抛出 `ModelNotFoundException`

**测试验证**：[CreateLoanTest.php:L78-L105](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/tests/Unit/Domains/Contact/ManageLoans/Services/CreateLoanTest.php#L78-L105) 中专门测试了跨 vault 贷款人和借贷人会抛出异常。

---

### 1.3 金额与货币解析机制

金额解析在控制器层完成，服务层只接收解析后的整数。

**控制器处理**（[ContactModuleLoanController.php:L36](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Domains/Contact/ManageLoans/Web/Controllers/ContactModuleLoanController.php#L36)）：

```php
$currency = $request->input('currency_id') ? Currency::find($request->input('currency_id')) : null;

$data = [
    // ...
    'amount_lent' => $request->input('amount_lent') 
        ? MonetaryNumberHelper::parseInput($request->input('amount_lent'), optional($currency)->code) 
        : null,
];
```

**解析核心**（[MonetaryNumberHelper.php:L104-L110](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Helpers/MonetaryNumberHelper.php#L104-L110)）：

```php
public static function parseInput(string $value, ?string $currency = null): int
{
    $moneyParser = new DecimalMoneyParser(static::getCurrencies());
    $money = $moneyParser->parse($value, new Currency($currency ?? 'USD'));

    return (int) $money->getAmount();
}
```

**解析流程**：
1. 使用 `moneyphp/money` 库的 `DecimalMoneyParser`
2. 根据货币代码确定精度（如 USD 是 2 位小数，JPY 是 0 位）
3. 将用户输入的十进制字符串（如 "145.00"）转换为以最小单位存储的整数（如 14500 分）
4. 数据库字段 `amount_lent` 为 `integer` 类型（[2022_03_23_005751_create_loans_table.php:L26](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/database/migrations/2022_03_23_005751_create_loans_table.php#L26)）

---

### 1.4 contact_loan Pivot 表双向写入机制

**数据模型设计**（[2022_03_23_005751_create_loans_table.php:L34-L39](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/database/migrations/2022_03_23_005751_create_loans_table.php#L34-L39)）：

```php
Schema::create('contact_loan', function (Blueprint $table) {
    $table->foreignIdFor(Loan::class)->constrained()->cascadeOnDelete();
    $table->foreignIdFor(Contact::class, 'loaner_id')->constrained('contacts')->cascadeOnDelete();
    $table->foreignIdFor(Contact::class, 'loanee_id')->constrained('contacts')->cascadeOnDelete();
    $table->timestamps();
});
```

**Pivot 表包含三个外键**：
- `loan_id`：关联贷款记录
- `loaner_id`：关联贷款人（借出方）
- `loanee_id`：关联借贷人（借入方）

**双向写入逻辑**（[CreateLoan.php:L96-L106](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Domains/Contact/ManageLoans/Services/CreateLoan.php#L96-L106)）：

```php
// 方向一：从贷款人角度关联
foreach ($this->loanersCollection as $loaner) {
    foreach ($this->loaneesCollection as $loanee) {
        $loaner->loansAsLoaner()->syncWithoutDetaching([
            $this->loan->id => ['loanee_id' => $loanee->id]
        ]);
    }
}

// 方向二：从借贷人角度关联
foreach ($this->loaneesCollection as $loanee) {
    foreach ($this->loanersCollection as $loaner) {
        $loanee->loansAsLoanee()->syncWithoutDetaching([
            $this->loan->id => ['loaner_id' => $loaner->id]
        ]);
    }
}
```

**关联关系定义**：
- Contact 模型：[loansAsLoaner](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Models/Contact.php#L246-L249) 和 [loansAsLoanee](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Models/Contact.php#L258-L261)
- Loan 模型：[loaners](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Models/Loan.php#L88-L91) 和 [loanees](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Models/Loan.php#L98-L101)

**设计意图**：
- 支持多对多关系（一笔贷款可有多个贷款人和多个借贷人）
- 双向写入确保从任一联系人角度都能查询到完整的贷款记录
- 使用 `syncWithoutDetaching` 避免重复关联

---

### 1.5 为什么只更新当前 contact 的 last_updated_at

在 [CreateLoan.php:L67-L68](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Domains/Contact/ManageLoans/Services/CreateLoan.php#L67-L68) 和 [ToggleLoan.php:L69-L70](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Domains/Contact/ManageLoans/Services/ToggleLoan.php#L69-L70) 中，只更新了当前 contact：

```php
$this->contact->last_updated_at = Carbon::now();
$this->contact->save();
```

**设计考量**：
1. **上下文关联性**：`$this->contact` 是 URL 中传入的 `contact_id`，即用户当前正在查看的联系人页面
2. **性能权衡**：一笔贷款可能关联多个联系人（多个 loaner + 多个 loanee），更新所有联系人会产生多次数据库写入
3. **功能需求**：`last_updated_at` 主要用于联系人列表的排序，标记"最近有活动"的联系人。从用户体验角度，当前查看的联系人需要立即反映更新
4. **潜在不足**：其他关联联系人的 `last_updated_at` 不会更新，可能导致其在"最近活动"排序中不准确

---

## 二、贷款状态切换（ToggleLoan）

### 2.1 状态切换核心逻辑

[ToggleLoan.php:L63-L71](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Domains/Contact/ManageLoans/Services/ToggleLoan.php#L63-L71)：

```php
private function toggle(): void
{
    $this->loan->settled = ! $this->loan->settled;
    $this->loan->settled_at = Carbon::now();
    $this->loan->save();

    $this->contact->last_updated_at = Carbon::now();
    $this->contact->save();
}
```

**字段说明**：
- `settled`：boolean 类型，默认 `false`（未结清），`true` 表示已结清
- `settled_at`：datetime 类型，记录状态变更时间

---

### 2.2 未结清状态展示机制

[ModuleLoanViewHelper.php:L17-L18](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Domains/Contact/ManageLoans/Web/ViewHelpers/ModuleLoanViewHelper.php#L17-L18)：

```php
$loansAsLoaner = $contact->loansAsLoaner()->where('settled', false)->get();
$loansAsLoanee = $contact->loansAsLoanee()->where('settled', false)->get();
```

**展示逻辑**：
1. 分别查询作为贷款人和借贷人的未结清贷款
2. 合并去重后按日期排序展示
3. 已结清贷款不在默认列表中显示

---

### 2.3 settled_at 残留问题分析

**问题**：从已结清切回未结清时，`settled_at` **会残留**之前的时间戳。

**原因分析**：
```php
$this->loan->settled = ! $this->loan->settled;  // 切换布尔值
$this->loan->settled_at = Carbon::now();        // 总是设置为当前时间，从不设为 null
```

**切换场景对比**：

| 切换方向 | settled 值 | settled_at 行为 | 预期行为 | 实际行为 |
|---------|-----------|----------------|---------|---------|
| 未结清 → 已结清 | false → true | 设置为当前时间 | ✅ 正确 | ✅ 记录结清时间 |
| 已结清 → 未结清 | true → false | 设置为当前时间 | ❌ 应设为 null | ❌ 残留/更新时间戳 |

**代码缺陷**：
当用户误操作将已结清贷款标记为未结清时，`settled_at` 字段仍然有值，这会导致：
1. 数据不一致性（未结清的贷款却有结清时间）
2. 前端展示 `settled_at_human_format` 时可能误导用户
3. 历史审计时无法准确知道何时真正结清

**建议修复方案**：
```php
private function toggle(): void
{
    $this->loan->settled = ! $this->loan->settled;
    $this->loan->settled_at = $this->loan->settled ? Carbon::now() : null;
    $this->loan->save();

    $this->contact->last_updated_at = Carbon::now();
    $this->contact->save();
}
```

---

## 三、MonetaryNumberHelper 金额输入边界分析

### 3.1 核心功能概述

[MonetaryNumberHelper.php](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Helpers/MonetaryNumberHelper.php) 是系统全局的金额处理工具类，基于 `moneyphp/money` 库实现。

---

### 3.2 金额存储格式

**设计原则**：所有金额以**整数形式存储最小货币单位**（如分、便士等）。

| 方法 | 作用 | 输入示例 | 输出示例 |
|------|------|---------|---------|
| `parseInput()` | 用户输入 → 存储整数 | "145.00", "USD" | 14500 |
| `inputValue()` | 存储整数 → 输入格式 | 14500, "USD" | "145.00" |
| `formatValue()` | 存储整数 → 显示格式（无符号） | 14500, "USD" | "1,234.50" |
| `format()` | 存储整数 → 显示格式（带符号） | 14500, "USD" | "$1,234.50" |

**解析边界**（[MonetaryNumberHelper.php:L104-L110](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Helpers/MonetaryNumberHelper.php#L104-L110)）：

```php
public static function parseInput(string $value, ?string $currency = null): int
{
    $moneyParser = new DecimalMoneyParser(static::getCurrencies());
    $money = $moneyParser->parse($value, new Currency($currency ?? 'USD'));

    return (int) $money->getAmount();
}
```

**货币精度边界**：
- USD（美元）：2 位小数 → 1.00 存储为 100
- JPY（日元）：0 位小数 → 1 存储为 1
- EUR（欧元）：2 位小数 → 1.00 存储为 100
- 精度由 ISO 4217 标准定义，通过 `ISOCurrencies` 获取

---

### 3.3 数字格式本地化

[MonetaryNumberHelper.php:L131-L165](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Helpers/MonetaryNumberHelper.php#L131-L165) 支持四种用户数字格式偏好：

| 格式类型 | 示例 | 千位分隔符 | 小数点 |
|---------|------|-----------|-------|
| `COMMA_THOUSANDS_DOT_DECIMAL` | 1,234.56 | `,` | `.` |
| `SPACE_THOUSANDS_COMMA_DECIMAL` | 1 234,56 | ` ` | `,` |
| `DOT_THOUSANDS_COMMA_DECIMAL` | 1.234,56 | `.` | `,` |
| `NO_SPACE_DOT_DECIMAL` | 1234.56 | `` | `.` |

**输入边界处理**：
1. 用户输入时需要先转换为标准十进制格式（使用 `.` 作为小数点，无千位分隔符）
2. `DecimalMoneyParser` 只接受标准格式如 "1234.56"
3. 格式化显示时再应用用户偏好的本地化格式

---

### 3.4 数据类型边界

**数据库层面**：
- `amount_lent` 字段定义为 `integer`（[迁移文件:L26](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/database/migrations/2022_03_23_005751_create_loans_table.php#L26)）
- MySQL 的 `integer` 是 32 位有符号整数，范围为 `-2,147,483,648` 到 `2,147,483,647`
- 对于 USD，最大可表示金额为 `$21,474,836.47`

**服务层验证**（[CreateLoan.php:L40](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Domains/Contact/ManageLoans/Services/CreateLoan.php#L40)）：
```php
'amount_lent' => 'nullable|integer',
```

**潜在边界问题**：
1. 缺少最大值验证（如防止输入超过 integer 上限的金额）
2. 缺少最小值验证（如不允许负数金额）
3. 对于日元等零精度货币，用户输入小数时会被截断（如输入 "1.5" JPY 会被解析为 1）

---

## 四、前端交互流程

[Loans.vue](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/resources/js/Shared/Modules/Loans.vue) 中的核心交互：

```javascript
// 创建贷款
const submit = () => {
  axios.post(props.data.url.store, form)
    .then((response) => {
      localLoans.value.unshift(response.data.data);
      createLoanModalShown.value = false;
    });
};

// 切换结清状态
const toggle = (loan) => {
  axios.put(loan.url.toggle, form)
    .then((response) => {
      localLoans.value[localLoans.value.findIndex((x) => x.id === loan.id)] = response.data.data;
    });
};
```

**状态切换后的前端处理**：
1. 调用 toggle 接口成功后，更新本地列表中的贷款数据
2. 由于当前列表只展示 `settled = false` 的贷款，结清后该贷款会从列表中消失
3. 若从已结清切回未结清（通过其他入口），贷款会重新出现在未结清列表中

---

## 五、代码优化建议

### 5.1 ToggleLoan settled_at 残留修复

```php
// ToggleLoan.php:L63-L71 修改建议
private function toggle(): void
{
    $this->loan->settled = ! $this->loan->settled;
    $this->loan->settled_at = $this->loan->settled ? Carbon::now() : null;
    $this->loan->save();

    $this->contact->last_updated_at = Carbon::now();
    $this->contact->save();
}
```

### 5.2 金额边界验证增强

在 CreateLoan 和 UpdateLoan 的 rules 中增加边界验证：

```php
'amount_lent' => 'nullable|integer|min:0|max:2147483647',
```

### 5.3 关联联系人时间戳更新考量

如需更新所有关联联系人的 `last_updated_at`，可添加：

```php
// 在 create() 方法最后添加
$allContacts = $this->loanersCollection->concat($this->loaneesCollection)->unique('id');
$allContacts->each(function ($contact) {
    $contact->last_updated_at = Carbon::now();
    $contact->save();
});
```

---

## 六、相关文件索引

| 功能模块 | 文件路径 |
|---------|---------|
| 创建贷款服务 | [CreateLoan.php](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Domains/Contact/ManageLoans/Services/CreateLoan.php) |
| 切换贷款状态服务 | [ToggleLoan.php](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Domains/Contact/ManageLoans/Services/ToggleLoan.php) |
| 金额处理工具 | [MonetaryNumberHelper.php](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Helpers/MonetaryNumberHelper.php) |
| Loan 模型 | [Loan.php](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Models/Loan.php) |
| Contact 模型 | [Contact.php](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Models/Contact.php) |
| 贷款控制器 | [ContactModuleLoanController.php](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Domains/Contact/ManageLoans/Web/Controllers/ContactModuleLoanController.php) |
| 状态切换控制器 | [ContactModuleToggleLoanController.php](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Domains/Contact/ManageLoans/Web/Controllers/ContactModuleToggleLoanController.php) |
| 视图数据组装 | [ModuleLoanViewHelper.php](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Domains/Contact/ManageLoans/Web/ViewHelpers/ModuleLoanViewHelper.php) |
| 数据库迁移 | [2022_03_23_005751_create_loans_table.php](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/database/migrations/2022_03_23_005751_create_loans_table.php) |
| 前端组件 | [Loans.vue](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/resources/js/Shared/Modules/Loans.vue) |
| 基础服务类 | [BaseService.php](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Services/BaseService.php) |
