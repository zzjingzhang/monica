# Monica 新用户注册流程完整链路分析

## 1. 注册流程总览

新用户从访问注册页面到拥有完整可用账户的完整链路如下：

```
访问注册页面 → 提交注册表单 → Fortify 动作处理 → 创建账户服务 → 用户与账户创建 → 异步初始化任务 → 账户就绪
```

---

## 2. 详细流程分解

### 2.1 注册入口层

#### 路由层 - 注册开关的第一道防线

**文件位置：**
- [FortifyServiceProvider.php](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Providers/FortifyServiceProvider.php#L67-L80)
- [bootstrap/app.php](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/bootstrap/app.php#L26-L31)

**实现机制：**

在 `FortifyServiceProvider::patchFortifyRoutes()` 方法中，系统动态地为 Fortify 的注册路由（`create` 显示页面、`store` 提交表单）添加 `monica.signup_is_enabled` 中间件别名。

该中间件别名映射到 `EnsureSignupIsEnabled` 中间件，在请求到达控制器之前拦截注册请求。

#### 控制器层 - 注册页面渲染

**文件位置：**
- [RegisterController.php](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Http/Controllers/Auth/RegisterController.php#L1-L30)

`RegisterController` 作为单动作控制器（Invoke 模式），负责：
1. 收集可用的社会化登录提供商
2. 渲染 `Auth/Register` 注册页面
3. 传递 beta 测试标记等前端状态

---

### 2.2 Fortify 动作层

**文件位置：**
- [CreateNewUser.php](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Actions/Fortify/CreateNewUser.php#L1-L36)

`CreateNewUser` 实现了 Laravel Fortify 的 `CreatesNewUsers` 契约，是注册流程的入口动作：

1. **验证用户输入**：姓名、邮箱、密码、服务条款
2. **委托创建**：调用 `CreateAccount` 服务执行实际的账户创建逻辑
3. **返回用户**：返回创建完成的 User 模型实例

---

### 2.3 账户创建服务层

**文件位置：**
- [CreateAccount.php](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Domains/Settings/CreateAccount/Services/CreateAccount.php#L1-L75)

`CreateAccount` 服务是核心业务逻辑层，执行以下同步操作：

#### 步骤 1：参数验证
验证用户的基本信息（姓名、邮箱、密码格式）

#### 步骤 2：创建 Account 账户实体
```php
$this->account = Account::create([
    'storage_limit_in_mb' => config('monica.default_storage_limit_in_mb'),
]);
```

#### 步骤 3：添加首个用户并授予管理员权限
```php
private function addFirstUser(): User
{
    return User::create([
        'account_id' => $this->account->id,
        'first_name' => $this->data['first_name'],
        'last_name' => $this->data['last_name'],
        'email' => $this->data['email'],
        'password' => Hash::make($this->data['password']),
        'locale' => App::getLocale(),
        'is_account_administrator' => true,  // 关键：首个用户自动获得管理员权限
        'timezone' => 'UTC',
    ]);
}
```

#### 步骤 4：触发异步初始化任务
```php
SetupAccount::dispatch($request)->onQueue('high');
```

---

### 2.4 初始化任务层（SetupAccount 异步任务）

**文件位置：**
- [SetupAccount.php](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Domains/Settings/CreateAccount/Jobs/SetupAccount.php#L1-L1217)

`SetupAccount` 是一个队列任务，负责为新账户填充所有必要的基础数据。任务在 `high` 优先级队列上异步执行。

#### 初始化内容分类：

##### A. 货币数据 (populateCurrencies)
- 从系统全局 `Currency` 表读取所有货币
- 将货币关联到当前账户（多对多关系）

##### B. 通知渠道 (addNotificationChannel)
- 创建基于用户注册邮箱的默认通知渠道
- 类型：`TYPE_EMAIL`
- 自动验证并激活（`verified_at = now()`, `active = true`）
- 默认提醒时间：09:00

##### C. 模板与页面结构
1. **创建默认模板**："Default template"（不可删除）
2. **模板页面与模块配置**：
   - **联系信息页 (Contact Information)**：头像、姓名、家庭概览、重要日期、性别/代词、标签、工作信息、宗教
   - **动态页 (Activity Feed)**：联系人动态流
   - **联系方式页 (Ways to connect)**：地址、联系信息
   - **社交页 (Social)**：关系、宠物、群组
   - **生活与目标页 (Life & goals)**：生活事件、目标
   - **信息页 (Information)**：文档、照片、笔记、提醒、借贷、任务、通话、日志

##### D. 基础字典数据 (addFirstInformation)

| 数据类型 | 默认项 |
|---------|--------|
| **性别 (Genders)** | Male, Female, Other |
| **代词 (Pronouns)** | he/him, she/her, they/them, per/per, ve/ver, xe/xem, ze/hir |
| **群组类型 (Group Types)** | 家庭（父/母、子/女）、伴侣（配偶）、俱乐部、协会、室友 |
| **关系类型 (Relationship Types)** | 爱情（6种）、家庭（6种）、朋友（2种）、工作（3种） |
| **地址类型 (Address Types)** | 家、第二住所、工作、度假屋、其他 |
| **通话原因 (Call Reasons)** | 个人类（5种）、商务类（2种） |
| **联系信息类型** | 邮箱、电话 + 配置的社交协议 |
| **宠物分类 (Pet Categories)** | 狗、猫、鸟、鱼、小动物、仓鼠、马、兔子、老鼠、爬行动物 |
| **情绪 (Emotions)** | 负面、中性、正面 |
| **礼物场合 (Gift Occasions)** | 生日、纪念日、圣诞节、随意、婚礼 |
| **礼物状态 (Gift States)** | 想法、搜索中、已找到、已购买、已送出 |
| **日志模板 (Post Templates)** | 普通日志、励志日志（含多个分区） |
| **宗教 (Religions)** | 基督教、伊斯兰教、印度教、佛教、神道教、道教、锡克教、犹太教、无神论 |

---

## 3. 注册开关的双重控制机制

### 3.1 路由层防护

**位置：** [EnsureSignupIsEnabled.php](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Http/Middleware/EnsureSignupIsEnabled.php#L1-L28)

**作用：**
- **第一道防线**：在 HTTP 请求层面拦截
- **提前拒绝**：请求到达业务逻辑前就返回 403 错误
- **性能优势**：无需加载业务服务即可判断
- **用户体验**：直接返回 "Registration is currently disabled" 提示

### 3.2 服务层逻辑判断

**位置：** [SignupHelper.php](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Helpers/SignupHelper.php#L1-L36)

**判断逻辑：**
```php
public function isEnabled(): bool
{
    return ! ($this->isDisabledByConfig() && $this->hasAtLeastOneAccount());
}
```

**判定条件解读：**

| 配置状态 | 是否有账户 | 注册是否启用 |
|---------|-----------|-------------|
| `disable_signup = false` | 任意 | ✅ 启用 |
| `disable_signup = true` | 无账户 | ✅ 启用（允许首个用户） |
| `disable_signup = true` | 有账户 | ❌ 禁用 |

**服务层作用：**
- **业务规则执行**：实现"配置禁用但允许首个用户"的复杂逻辑
- **可复用性**：可被命令行、API、其他服务调用
- **状态一致性**：确保所有入口点遵循相同的注册判定规则

---

## 4. 首个用户管理员权限机制

### 4.1 权限授予点

**位置：** [CreateAccount.php](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Domains/Settings/CreateAccount/Services/CreateAccount.php#L52-L64)

在 `addFirstUser()` 方法中硬编码设置：
```php
'is_account_administrator' => true,
```

### 4.2 设计特点

1. **隐式授予**：不通过独立服务调用，而是在用户创建时直接赋值
2. **仅限首个用户**：后续通过邀请加入的用户默认不具备此权限
3. **字段级权限**：使用 `is_account_administrator` 布尔字段标记
4. **不可撤销（创建时）**：创建后可通过 `GiveAdministratorPrivilege` / `RemoveAdministratorPrivilege` 服务变更

### 4.3 权限字段位置

**位置：** [User.php](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Models/User.php#L84-L100)

`is_account_administrator` 位于 `$fillable` 数组中，允许批量赋值。

---

## 5. SetupAccount 异步执行的可见一致性风险

### 5.1 时间窗口问题

**风险场景：**
用户注册成功后立即登录，此时 `SetupAccount` 任务可能仍在队列中执行或尚未完成。

**典型时间线：**
```
T0: 用户提交注册表单
T1: Account + User 创建完成 → 用户自动登录
T2: SetupAccount 任务派发到队列
T3: 用户跳转到仪表盘 / 联系人页面
T4: SetupAccount 开始执行（高优先级队列）
T5: SetupAccount 执行完成
```

**问题：** T3 时刻用户已登录，但 T5 时刻数据才就绪

### 5.2 具体风险点

| 风险类型 | 影响页面 / 功能 | 表现形式 |
|---------|----------------|---------|
| **模板缺失** | 联系人详情页 | 页面空白、模块不显示、Vue 组件报错 |
| **模块缺失** | 所有联系人相关页面 | 无法添加地址、电话等信息 |
| **字典数据缺失** | 联系人编辑表单 | 下拉框为空、无法选择性别/关系类型 |
| **货币缺失** | 设置 / 礼品页面 | 货币选择器空白 |
| **通知渠道缺失** | 提醒功能 | 无法设置生日提醒、纪念日提醒 |
| **宠物分类缺失** | 宠物模块 | 无法创建宠物记录 |

### 5.3 数据一致性问题

初始化任务是**事务外**执行的，如果中途失败：
- 账户和用户已存在（同步创建）
- 部分初始化数据已插入
- 后续数据未插入
- 无回滚机制（任务失败不会删除账户）

---

## 6. 初始化任务失败的异常暴露点

### 6.1 最先暴露异常的页面

**按概率排序：**

#### 1. 联系人创建 / 编辑页面
**暴露原因：** 依赖最多的初始化数据
- 模板页面结构不存在
- 模块未关联
- 性别、代词下拉为空
- 地址类型不存在

**文件关联：** 联系人模块的所有 Vue 页面

#### 2. 联系人列表页 / Vault 首页
**暴露原因：** 需要默认模板来渲染联系人卡片
- 无法显示联系人基本信息
- 头像、姓名模块缺失

#### 3. 设置页面
**暴露原因：** 尝试读取账户配置时触发
- 通知渠道列表为空
- 模板管理页异常
- 货币设置页异常

#### 4. 关系 / 群组功能
**暴露原因：** 依赖关系类型和群组类型字典
- 无法创建关系
- 群组类型选择器为空

### 6.2 最先暴露异常的服务

| 服务名称 | 触发场景 | 异常类型 |
|---------|---------|---------|
| **联系人创建服务** | 用户尝试添加第一个联系人时 | 找不到默认模板 / 模块 |
| **提醒服务** | 系统尝试发送每日提醒时 | 找不到活跃的通知渠道 |
| **礼品模块** | 记录礼物想法时 | 礼物状态 / 场合字典为空 |
| **日志功能** | 发布第一篇日志时 | 日志模板不存在 |
| **情绪跟踪** | 记录活动情绪时 | 情绪类型缺失 |

### 6.3 隐性异常（不易察觉）

1. **货币关联失败**：设置页显示货币选择器为空，但不报错
2. **宠物分类缺失**：宠物模块无法使用，但用户可能暂时不用
3. **通话原因缺失**：通话记录功能异常，但属于低频功能

---

## 7. 关键代码引用汇总

| 组件 | 文件路径 |
|------|---------|
| 注册入口控制器 | [RegisterController.php](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Http/Controllers/Auth/RegisterController.php) |
| Fortify 注册动作 | [CreateNewUser.php](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Actions/Fortify/CreateNewUser.php) |
| 账户创建服务 | [CreateAccount.php](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Domains/Settings/CreateAccount/Services/CreateAccount.php) |
| 初始化任务 | [SetupAccount.php](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Domains/Settings/CreateAccount/Jobs/SetupAccount.php) |
| 注册开关中间件 | [EnsureSignupIsEnabled.php](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Http/Middleware/EnsureSignupIsEnabled.php) |
| 注册辅助类 | [SignupHelper.php](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Helpers/SignupHelper.php) |
| Fortify 路由补丁 | [FortifyServiceProvider.php](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Providers/FortifyServiceProvider.php#L67-L80) |
| 用户模型 | [User.php](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/app/Models/User.php) |
| 应用配置 | [monica.php](file:///Users/zhangjing/Desktop/so-coders/0508-und-p/monica/config/monica.php) |
